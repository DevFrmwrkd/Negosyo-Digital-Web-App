---
/**
 * autoshop / FaqAF — "straight answers" accordion. Uses the shared
 * details/summary primitives (white sheet rows, red ▸ arrow that rotates to ▾
 * on open). Reads content.faq.items.
 */
interface Props { content: any; }
const { content } = Astro.props;
const faq = content.faq || {};
const tag = faq.tag || '';
const headline = faq.headline || '';
type Item = { q: string; a: string };
const items: Item[] = Array.isArray(faq.items) ? faq.items : (Array.isArray(faq) ? faq : []);
---
{items.length > 0 && (
  <section class="faq-sect">
    <div class="wrap faq-wrap">
      {tag && <span class="tag" data-field="faq.tag">{tag}</span>}
      {headline && <h2 class="faq-h" data-field="faq.headline" set:html={headline}></h2>}
      <div class="faq-list">
        {items.map((item, i) => (
          <details class="reveal">
            <summary><span data-field={`faq.items.${i}.q`}>{item.q}</span></summary>
            <div class="faq-a" data-field={`faq.items.${i}.a`} set:html={item.a}></div>
          </details>
        ))}
      </div>
    </div>
  </section>
)}
<style>
  .faq-wrap { max-width: 760px; }
  .faq-sect .tag { margin-bottom: 14px; }
  .faq-h { font-size: clamp(32px, 4.5vw, 52px); margin: 0 0 22px; color: var(--ink); }
</style>
