---
/**
 * autoshop / CtaBandAP — full-width amber closer, centred: "get an honest
 * quote". Amber ground with a burnt-amber kicker, oversized stencil headline
 * and up to three CTAs (ink fill + two ink ghosts, recoloured for the amber
 * band in PageAP's global block). The section anchors #quote for the hero /
 * nav CTA. cta1 is the message action, cta2 the call action.
 */
interface Props { content: any; layout: any; }
const { content, layout } = Astro.props;
const cta = content.ctaBand || content.closing || {};
const tag = cta.tag || '';
const headline = cta.headline || '';
const sub = cta.sub || '';
const cta1Text = cta.cta1?.text || '';
const cta1Href = cta.cta1?.href || '#location';
const cta2Text = cta.cta2?.text || '';
const cta2Href = cta.cta2?.href || (layout.contact?.phone ? `tel:${String(layout.contact.phone).replace(/[^0-9+]/g, '')}` : '');
const cta3Text = cta.cta3?.text || '';
const cta3Href = cta.cta3?.href || '#location';
const hasButtons = !!(cta1Text || cta2Text || cta3Text);
---
{(headline || hasButtons) && (
  <section class="sec cta-band" id="quote">
    <div class="wrap">
      {tag && <div class="kicker" data-field="ctaBand.tag">{tag}</div>}
      {headline && <h2 class="cta-h reveal" data-field="ctaBand.headline" set:html={headline}></h2>}
      {sub && <p class="cta-sub" data-field="ctaBand.sub">{sub}</p>}
      {hasButtons && (
        <div class="cta-row">
          {cta1Text && <a href={cta1Href} class="btn btn--fill" rel="noopener" data-field="ctaBand.cta1.text" data-href-field="ctaBand.cta1.href">{cta1Text}</a>}
          {cta2Text && <a href={cta2Href} class="btn btn--ghost" rel="noopener" data-field="ctaBand.cta2.text" data-href-field="ctaBand.cta2.href">{cta2Text}</a>}
          {cta3Text && <a href={cta3Href} class="btn btn--ghost" rel="noopener" data-field="ctaBand.cta3.text" data-href-field="ctaBand.cta3.href">{cta3Text}</a>}
        </div>
      )}
    </div>
  </section>
)}
<style>
  .cta-band { background: var(--amber); color: #0b0b0b; text-align: center; }
  .cta-band .kicker { color: #3a1a00; }
  .cta-h { font-size: clamp(40px,7vw,96px); color: #0b0b0b; margin: .18em 0 18px; }
  .cta-sub { color: #1a0d00; opacity: .85; max-width: 52ch; margin: 0 auto 18px; }
  .cta-row { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; }
</style>
