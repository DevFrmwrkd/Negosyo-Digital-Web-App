---
/**
 * ServicesC - Card Grid
 * White background. Centered badge text + headline. Grid of service cards with icons.
 * 3 columns desktop, 2 tablet, 1 mobile. Hover shadow + lift animation.
 * No images.
 */
interface Props {
  headline?: string;
  subheadline?: string;
  services: Array<{
    name: string;
    description: string;
    icon?: string;
  }>;
  photos: string[];
  visibility?: Record<string, boolean>;
}

const {
  headline = 'What We Offer',
  services = [],
  photos = [],
  visibility,
} = Astro.props;

// Visibility defaults (all visible unless explicitly false)
const showBadge = visibility?.servicesBadge !== false;
const showHeadline = visibility?.servicesHeadline !== false;
const showList = visibility?.servicesList !== false;

// 6 inline SVG icons (24x24 viewBox) cycled per service
const serviceIcons: string[] = [
  // 1. Wrench/tool
  `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>`,
  // 2. Star
  `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>`,
  // 3. Lightning/bolt
  `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>`,
  // 4. Heart
  `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>`,
  // 5. Shield
  `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>`,
  // 6. Gear/settings
  `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>`,
];
---

<section id="services" class="reveal bg-[var(--bg)] py-24 md:py-32 lg:py-48 relative overflow-hidden">
  {/* Modern background element */}
  <div class="absolute inset-x-0 top-0 h-[60%] bg-gradient-to-b from-[var(--primary)]/5 to-transparent pointer-events-none"></div>

  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 relative z-10">

    {/* Header */}
    <div class="reveal mx-auto mb-20 max-w-4xl text-center md:mb-32">
      {showBadge && (
        <div class="mb-8 flex justify-center">
          <span class="inline-block rounded-full bg-[var(--primary)]/10 px-8 py-3 text-[var(--fs-xs)] font-bold uppercase tracking-[0.4em] text-[var(--primary)] border border-[var(--primary)]/20 shadow-xl backdrop-blur-md">
            Capabilities
          </span>
        </div>
      )}

      {showHeadline && (
        <h2
          class="font-black leading-[1.05] tracking-tighter text-[var(--text)]"
          style="font-size: var(--fs-display); font-family: var(--heading-font, inherit);"
        >
          {headline}
        </h2>
      )}
    </div>

    {/* Card Grid */}
    {showList && (
      <div class="grid grid-cols-1 gap-10 sm:grid-cols-2 lg:grid-cols-3">
        {services.map((service, index) => (
          <div 
            class="reveal services-c-card group relative rounded-[3rem] border border-[var(--text)]/5 bg-[var(--surface)] p-10 transition-all duration-700 hover:-translate-y-4 hover:shadow-[0_40px_80px_rgba(0,0,0,0.1)] md:p-12"
            style={`transition-delay: ${index * 0.15}s;`}
          >
            {/* Glow effect */}
            <div class="absolute -inset-2 bg-gradient-to-br from-[var(--primary)]/20 to-transparent blur-3xl rounded-[3rem] opacity-0 transition-opacity duration-700 group-hover:opacity-100"></div>

            {/* Icon circle */}
            <div
              class="relative z-10 mb-10 flex h-20 w-20 items-center justify-center rounded-[1.5rem] bg-[var(--primary)] text-[var(--text-inverse)] shadow-lg shadow-[var(--primary)]/30 transition-all duration-700 group-hover:rotate-[15deg] group-hover:scale-110"
              set:html={serviceIcons[index % serviceIcons.length]}
            />

            {/* Name */}
            <h3
              class="relative z-10 mb-6 font-black text-[var(--text)] tracking-tight"
              style="font-size: var(--fs-xl); font-family: var(--heading-font, inherit);"
            >
              {service.name}
            </h3>

            {/* Description */}
            <p class="relative z-10 leading-[1.6] text-[var(--text-muted)] font-medium" style="font-size: var(--fs-base);">
              {service.description}
            </p>
            
            {/* Corner arrow */}
            <div class="absolute top-10 right-10 opacity-0 transition-all duration-700 group-hover:opacity-100 group-hover:translate-x-2 group-hover:-translate-y-2">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--primary)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                <path d="M7 7h10v10" /><path d="M7 17L17 7" />
              </svg>
            </div>
          </div>
        ))}
      </div>
    )}
  </div>
</section>
